# ==========================================
# CONFIGURAÇÃO DE PRODUÇÃO
# ==========================================
# Copie este arquivo para .env e configure com seus dados reais

# BANCO DE DADOS (substitua pelos dados da sua hospedagem)
DB_HOST=localhost
DB_USER=seu_usuario_mysql
DB_PASSWORD=sua_senha_mysql
DB_NAME=encurtador_links
DB_PORT=3306

# SERVIDOR
PORT=8080

# SEGURANÇA JWT 
# IMPORTANTE: Gere uma chave secreta forte e única!
# Para gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua-chave-secreta-super-forte-aqui-mude-isto-em-producao
JWT_EXPIRES_IN=24h

# AMBIENTE
NODE_ENV=production

# ==========================================
# CONFIGURAÇÃO DE DESENVOLVIMENTO (opcional)
# ==========================================
# Para desenvolvimento local, use:
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=
# NODE_ENV=development